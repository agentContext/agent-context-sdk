/**
 * Agent App & Guide Public API
 * 基本地址：`/v1/`（已在 servers 配置） - 普通接口 **无鉴权** - 统一错误：`{\"error\":\"reason\"}` - 分页：`page` 默认 1，`limit` 默认 20 - `score` 为相似度（仅 /guide/search/{pkgname} 返回） 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Guide from './Guide';
import Paging from './Paging';

/**
 * The GuideSearchResponse model module.
 * @module model/GuideSearchResponse
 * @version 1.0.0
 */
class GuideSearchResponse {
    /**
     * Constructs a new <code>GuideSearchResponse</code>.
     * @alias module:model/GuideSearchResponse
     * @param data {Array.<module:model/Guide>} 
     * @param paging {module:model/Paging} 
     * @param score {Array.<Number>} 与 data 同顺序的相似度分数
     */
    constructor(data, paging, score) { 
        
        GuideSearchResponse.initialize(this, data, paging, score);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, paging, score) { 
        obj['data'] = data;
        obj['paging'] = paging;
        obj['score'] = score;
    }

    /**
     * Constructs a <code>GuideSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GuideSearchResponse} obj Optional instance to populate.
     * @return {module:model/GuideSearchResponse} The populated <code>GuideSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GuideSearchResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [Guide]);
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = Paging.constructFromObject(data['paging']);
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GuideSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GuideSearchResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GuideSearchResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                Guide.validateJSON(item);
            };
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          Paging.validateJSON(data['paging']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['score'])) {
            throw new Error("Expected the field `score` to be an array in the JSON data but got " + data['score']);
        }

        return true;
    }


}

GuideSearchResponse.RequiredProperties = ["data", "paging", "score"];

/**
 * @member {Array.<module:model/Guide>} data
 */
GuideSearchResponse.prototype['data'] = undefined;

/**
 * @member {module:model/Paging} paging
 */
GuideSearchResponse.prototype['paging'] = undefined;

/**
 * 与 data 同顺序的相似度分数
 * @member {Array.<Number>} score
 */
GuideSearchResponse.prototype['score'] = undefined;






export default GuideSearchResponse;

