#' Agent App & Guide Public API
#'
#' 基本地址：`/v1/`（已在 servers 配置） - 普通接口 **无鉴权** - 统一错误：`{\"error\":\"reason\"}` - 分页：`page` 默认 1，`limit` 默认 20 - `score` 为相似度（仅 /guide/search/{pkgname} 返回） 
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Guide operations
#' @description GuideApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GuideGet  ####################
#'
#' library(agent-context-sdk)
#' var_q <- "q_example" # character | 搜索关键词
#' var_page <- 1 # integer | 页码（默认 1） (Optional)
#' var_limit <- 20 # integer | 每页数量（默认 20） (Optional)
#'
#' #获取 guide 列表（按关键词）
#' api_instance <- GuideApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GuideGet(var_q, page = var_page, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GuideGet(var_q, page = var_page, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GuideIdIdGet  ####################
#'
#' library(agent-context-sdk)
#' var_id <- 56 # integer | 
#'
#' #获取指定 guide（按 id）
#' api_instance <- GuideApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GuideIdIdGet(var_iddata_file = "result.txt")
#' result <- api_instance$GuideIdIdGet(var_id)
#' dput(result)
#'
#'
#' ####################  GuidePkgPkgnameGet  ####################
#'
#' library(agent-context-sdk)
#' var_pkgname <- "pkgname_example" # character | 
#' var_page <- 1 # integer | 页码（默认 1） (Optional)
#' var_limit <- 20 # integer | 每页数量（默认 20） (Optional)
#'
#' #获取某包名下的 guide 列表
#' api_instance <- GuideApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GuidePkgPkgnameGet(var_pkgname, page = var_page, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GuidePkgPkgnameGet(var_pkgname, page = var_page, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GuideSearchPkgnameGet  ####################
#'
#' library(agent-context-sdk)
#' var_pkgname <- "pkgname_example" # character | 
#' var_q <- "q_example" # character | 搜索关键词
#' var_page <- 1 # integer | 页码（默认 1） (Optional)
#' var_limit <- 20 # integer | 每页数量（默认 20） (Optional)
#'
#' #在指定包下根据关键词搜索 guide（词向量相似度）
#' api_instance <- GuideApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GuideSearchPkgnameGet(var_pkgname, var_q, page = var_page, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GuideSearchPkgnameGet(var_pkgname, var_q, page = var_page, limit = var_limit)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
GuideApi <- R6::R6Class(
  "GuideApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new GuideApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 获取 guide 列表（按关键词）
    #'
    #' @param q 搜索关键词
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GuideListResponse
    GuideGet = function(q, page = 1, limit = 20, data_file = NULL, ...) {
      local_var_response <- self$GuideGetWithHttpInfo(q, page, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 获取 guide 列表（按关键词）
    #'
    #' @param q 搜索关键词
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GuideListResponse) with additional information such as HTTP status code, headers
    GuideGetWithHttpInfo = function(q, page = 1, limit = 20, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (nchar(`q`) < 1) {
        stop("Invalid length for `q` when calling GuideApi$GuideGet, must be bigger than or equal to 1.")
      }

      if (`page` < 1) {
        stop("Invalid value for `page` when calling GuideApi$GuideGet, must be bigger than or equal to 1.")
      }

      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling GuideApi$GuideGet, must be bigger than or equal to 1.")
      }

      query_params[["q"]] <- `q`

      query_params[["page"]] <- `page`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/guide"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GuideListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 获取指定 guide（按 id）
    #'
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GuideItemResponse
    GuideIdIdGet = function(id, data_file = NULL, ...) {
      local_var_response <- self$GuideIdIdGetWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 获取指定 guide（按 id）
    #'
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GuideItemResponse) with additional information such as HTTP status code, headers
    GuideIdIdGetWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/guide/id/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GuideItemResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 获取某包名下的 guide 列表
    #'
    #' @param pkgname 
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GuideListResponse
    GuidePkgPkgnameGet = function(pkgname, page = 1, limit = 20, data_file = NULL, ...) {
      local_var_response <- self$GuidePkgPkgnameGetWithHttpInfo(pkgname, page, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 获取某包名下的 guide 列表
    #'
    #' @param pkgname 
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GuideListResponse) with additional information such as HTTP status code, headers
    GuidePkgPkgnameGetWithHttpInfo = function(pkgname, page = 1, limit = 20, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`pkgname`)) {
        stop("Missing required parameter `pkgname`.")
      }


      if (`page` < 1) {
        stop("Invalid value for `page` when calling GuideApi$GuidePkgPkgnameGet, must be bigger than or equal to 1.")
      }

      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling GuideApi$GuidePkgPkgnameGet, must be bigger than or equal to 1.")
      }

      query_params[["page"]] <- `page`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/guide/pkg/{pkgname}"
      if (!missing(`pkgname`)) {
        local_var_url_path <- gsub("\\{pkgname\\}", URLencode(as.character(`pkgname`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GuideListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 在指定包下根据关键词搜索 guide（词向量相似度）
    #'
    #' @param pkgname 
    #' @param q 搜索关键词
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GuideSearchResponse
    GuideSearchPkgnameGet = function(pkgname, q, page = 1, limit = 20, data_file = NULL, ...) {
      local_var_response <- self$GuideSearchPkgnameGetWithHttpInfo(pkgname, q, page, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 在指定包下根据关键词搜索 guide（词向量相似度）
    #'
    #' @param pkgname 
    #' @param q 搜索关键词
    #' @param page (optional) 页码（默认 1） (default value: 1)
    #' @param limit (optional) 每页数量（默认 20） (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GuideSearchResponse) with additional information such as HTTP status code, headers
    GuideSearchPkgnameGetWithHttpInfo = function(pkgname, q, page = 1, limit = 20, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`pkgname`)) {
        stop("Missing required parameter `pkgname`.")
      }

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }


      if (nchar(`q`) < 1) {
        stop("Invalid length for `q` when calling GuideApi$GuideSearchPkgnameGet, must be bigger than or equal to 1.")
      }

      if (`page` < 1) {
        stop("Invalid value for `page` when calling GuideApi$GuideSearchPkgnameGet, must be bigger than or equal to 1.")
      }

      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling GuideApi$GuideSearchPkgnameGet, must be bigger than or equal to 1.")
      }

      query_params[["q"]] <- `q`

      query_params[["page"]] <- `page`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/guide/search/{pkgname}"
      if (!missing(`pkgname`)) {
        local_var_url_path <- gsub("\\{pkgname\\}", URLencode(as.character(`pkgname`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GuideSearchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
